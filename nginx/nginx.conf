# # nginx/nginx.conf
# worker_processes 1;

# events {
#     worker_connections 1024;
# }

# http {
#     server {
#         listen 80;

#         server_name 157.230.87.36;  # Replace with your domain or public IP

#         location / {
#             proxy_pass http://157.230.87.36:3000;  # Send requests to React frontend
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#         }

#         location /api/ {
#             proxy_pass http://157.230.87.36:8000/;  # Send API requests to Django
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#         }
#     }
# }

worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        server_name 157.230.87.36;  # Your public IP or domain

        location / {
            proxy_pass http://react-app:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /api/ {
            proxy_pass http://django-web:8000/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;

            # ✅ Fix CORS issue by adding these headers
            add_header Access-Control-Allow-Origin *;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE";
            add_header Access-Control-Allow-Headers "Origin, X-Requested-With, Content-Type, Accept, Authorization";

            # ✅ Handle preflight OPTIONS requests
            if ($request_method = OPTIONS) {
                add_header Content-Length 0;
                add_header Content-Type text/plain;
                return 204;
            }
        }
    }
}

